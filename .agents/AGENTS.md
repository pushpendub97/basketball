# Agent Rules for Basketball Workspace

- **Code Suppressed in Output**: Never include R or code blocks in the rendered documents or display source code unless explicitly requested otherwise by the user. Ensure all execution settings in QMD/Quarto files are set to `echo: false` by default.
